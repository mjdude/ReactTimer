var expect = require('expect');

// Second argument of it is a function, this this example im using ES6 arrow function as a shortcut

it('it should properly run tests', () => {
  expect(1).toBe(1);
});
